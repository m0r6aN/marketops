import Link from "next/link";import { Badge } from "@/components/ui/badge";import { buttonVariants } from "@/components/ui/button";import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";import type { CitationReadinessRecord } from "@/lib/citation-readiness/types";export function CitationReadinessSummaryCard({initiativeSlug,versions}:{initiativeSlug:string;versions:CitationReadinessRecord[]}){const items=new Set(versions.map((item)=>item.citationPlanItemId)).size,approved=versions.filter((item)=>item.status==="approved").length;return <Card><CardHeader className="flex-row items-start justify-between gap-3"><div><CardTitle className="text-base">AI citation readiness</CardTitle><CardDescription>Source-mapped answer, entity, authorship, freshness, structure, and monitoring plans derived from audit evidence.</CardDescription></div><Link href={`/initiatives/${initiativeSlug}/citation-readiness`} className={buttonVariants({size:"sm"})}>{items?"Open plans":"Create plan"}</Link></CardHeader><CardContent className="flex gap-2"><Badge variant="outline">{items} plan{items===1?"":"s"}</Badge><Badge variant="outline">{approved} approved</Badge></CardContent></Card>;}

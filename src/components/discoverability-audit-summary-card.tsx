import Link from "next/link";import { Badge } from "@/components/ui/badge";import { buttonVariants } from "@/components/ui/button";import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";import type { DiscoverabilityAuditRecord } from "@/lib/discoverability-audits/types";export function DiscoverabilityAuditSummaryCard({initiativeSlug,audits}:{initiativeSlug:string;audits:DiscoverabilityAuditRecord[]}){const succeeded=audits.filter((item)=>item.status==="succeeded").length;return <Card><CardHeader className="flex-row items-start justify-between gap-3"><div><CardTitle className="text-base">SEO / AEO / GEO audits</CardTitle><CardDescription>Evidence snapshots and heuristic coverage checks for search, answer engines, and source clarity.</CardDescription></div><Link href={`/initiatives/${initiativeSlug}/discoverability-audits`} className={buttonVariants({size:"sm"})}>{audits.length?"Open audits":"Run audit"}</Link></CardHeader><CardContent className="flex gap-2"><Badge variant="outline">{audits.length} run{audits.length===1?"":"s"}</Badge><Badge variant="outline">{succeeded} analyzed</Badge></CardContent></Card>;}

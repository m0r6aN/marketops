import Link from "next/link";import { Badge } from "@/components/ui/badge";import { buttonVariants } from "@/components/ui/button";import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";import type { EmailCampaignVersionRecord } from "@/lib/email-campaigns/types";
export function EmailCampaignSummaryCard({initiativeSlug,versions}:{initiativeSlug:string;versions:EmailCampaignVersionRecord[]}){const items=new Set(versions.map((item)=>item.emailCampaignItemId)).size,approved=versions.filter((item)=>item.status==="approved").length;return <Card><CardHeader className="flex-row items-start justify-between gap-3"><div><CardTitle className="text-base">Email campaign builder</CardTitle><CardDescription>Source-backed sequences with audience, sender, consent, suppression, copy, CTA, and measurement planning.</CardDescription></div><Link href={`/initiatives/${initiativeSlug}/email-campaigns`} className={buttonVariants({size:"sm"})}>{items?"Open email campaigns":"Build email campaign"}</Link></CardHeader><CardContent className="flex gap-2"><Badge variant="outline">{items} campaign{items===1?"":"s"}</Badge><Badge variant="outline">{approved} approved</Badge></CardContent></Card>;}

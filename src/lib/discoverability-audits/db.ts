import { db } from "@/lib/content-workspace/db";db.exec(`CREATE TABLE IF NOT EXISTS discoverability_audits(id TEXT PRIMARY KEY,initiative_slug TEXT NOT NULL,requested_url TEXT NOT NULL,final_url TEXT NOT NULL,source_mode TEXT NOT NULL,status TEXT NOT NULL,provider TEXT NOT NULL,provider_version TEXT NOT NULL,content_hash TEXT NOT NULL,html_snapshot TEXT NOT NULL,seo_coverage INTEGER NOT NULL,aeo_coverage INTEGER NOT NULL,geo_coverage INTEGER NOT NULL,findings_json TEXT NOT NULL,error_message TEXT NOT NULL,created_at TEXT NOT NULL);CREATE INDEX IF NOT EXISTS idx_discoverability_audits_initiative ON discoverability_audits(initiative_slug,created_at DESC);`);export{db};

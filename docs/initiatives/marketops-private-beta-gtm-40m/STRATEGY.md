# Strategy — marketops-private-beta-gtm-40m

## Purpose
Prove faster approvals + traceable evidence for regulated marketing teams in access-restricted beta, to earn hybrid $40M (revenue + NRR/expansion + defensible IP).

## Preserve
Canon Foundry conflict/version/provenance, Docs-as-Marketing schema, `dry_run` law, immutable versions, fail-closed (never claim sent/published/ranked), proofpack verify chain.

## Fix
auth/tenant, Postgres/RLS/backups, vault/fail-closed, claim eval, compliance enforcement, headers/rate-limit, CI, public proof + pricing/Stripe, traction.

## Waves + exits
- W0 Contracts + Spike — contracts PR merged, spike proves tenant-scoped write + denial receipt.
- W1 Foundation (7 parallel) — each parcel independently reviewable + verified.
- W2 Assembly (serialized) — beta flows wire without touching >1 serialization point at once.
- INT Integration — matrix all-pass in `local` + `beta` with evidence.
- H Hardening — security gates pass, no-send still enforced.
- R Release — private-beta approved, valuation packet credible for Milestone A ($18–22M) → B ($40M).

## Ready means
Keon/BioStack dogfood in beta with tenant isolation + receipts + pilot contracts, zero public SaaS exposure, evidence > claims.

Source of truth: `.coordinator/coordinator.db`. This file is a generated view.
